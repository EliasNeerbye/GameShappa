import "../css/components/NavBar.css";

export default function NavBar() {
    return (
        <>
            <nav>
                <a href="#">Home</a>
                <a href="#">About</a>
                <a href="#">Shop</a>
                <a href="#">Login</a>
            </nav>
        </>
    );
}
